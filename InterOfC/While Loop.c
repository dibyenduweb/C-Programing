//While loops
#include <stdio.h>

int main() {
    int num = 1;

    // Using a while loop to increment num until it reaches 10
    while (num < 10) {
        printf("Current value of i: %d\n", num);
        num++; // Increment i by 1
    }

    return 0;
}